# Common environment for dev/xds-gateway stack
# Policy Service (xds-gateway)
GRPC_PORT=8081
HTTP_ADDR=:8080

# Redis connection for gateway and xds services
REDIS_ADDR=redis:6379
REDIS_DB=0
# REDIS_PASSWORD=   # uncomment if needed

# Caching TTLs (seconds)
CACHE_TTL_SECONDS=60
NEGATIVE_CACHE_TTL_SECONDS=10

# Default route plane id for gateway resolve fallback
# Note: plane registry should contain a plane with id "default" and Enabled=true
# but even if not set, Envoy static default cluster xds_default will be used (fail-open)
DEFAULT_PLANE_ID=default

# REST API auth for gateway (empty disables auth)
# AUTH_TOKEN=devtoken
